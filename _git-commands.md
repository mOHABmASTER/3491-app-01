# Initiate a project
```bash
git init
```

# Add file to the staged files
```bash
git add <file>
```

# Add all changed files to the stage
```bash
git add .
```

# Create a commit with staged changes
```bash
git commit -m "My Message"
```

# List all commits
```bash
git log
```